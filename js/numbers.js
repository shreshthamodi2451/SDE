const score=400
// console.log(score)

const score2= new Number(20000)
// console.log(score2)

const name="shreshtha"
// console.log(name)

const name2= new String("modi")
// console.log(name2)

console.log(score2.toFixed(2))//gives 400.00 because it converts the number to a string with 2 decimal places   
//tolocalestring puts commas betwenn numbers and also converts to string
console.log(score2.toLocaleString())//gives 400 because it converts the number to a string with commas between numbers

